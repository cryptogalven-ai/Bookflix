(()=>{'use strict';if(window.__bookflixAppHealthy)return;const KEY='bookflix_v4';const $=id=>document.getElementById(id);const norm=s=>String(s||'').toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/[^a-z0-9]+/g,' ').trim();const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const seeds=[['Neuromancer','William Gibson'],['Dune','Frank Herbert'],['1984','George Orwell'],['Ubik','Philip K. Dick'],['Fondation','Isaac Asimov'],['Silo','Hugh Howey']].map(x=>({title:x[0],author:x[1],cover:''}));let raw={};try{raw=JSON.parse(localStorage.getItem(KEY)||'{}')}catch{}const S={selected:Array.isArray(raw.selected)?raw.selected:[],current:[]};function render(list){S.current=list;$('books').innerHTML=list.length?list.map((b,i)=>`<article class="book ${S.selected.some(x=>norm(x.title)===norm(b.title))?'sel':''}" data-rx="${i}"><div class="cover" ${b.cover?`style="background-image:url('${b.cover}')"`:''}>${b.cover?'':esc(b.title)}</div><small><b>${esc(b.title)}</b><br>${esc(b.author)}</small></article>`).join(''):'<div class="empty">Aucun livre trouvé.</div>';document.querySelectorAll('[data-rx]').forEach(e=>e.onclick=()=>{const b=S.current[+e.dataset.rx];if(!b)return;const i=S.selected.findIndex(x=>norm(x.title)===norm(b.title));if(i>=0)S.selected.splice(i,1);else if(S.selected.length<8)S.selected.push(b);render(S.current)});if($('count'))$('count').textContent=S.selected.length+' / 3 minimum';if($('chips'))$('chips').innerHTML=S.selected.map(b=>`<span class="chip">✓ ${esc(b.title)}</span>`).join('')}async function search(){const q=($('q')?.value||'').trim();if(!q)return render(seeds);try{const r=await fetch('/api/openlibrary/search?q='+encodeURIComponent(q)+'&limit=24');const j=await r.json();render((j.docs||[]).map(d=>({title:d.title||'Sans titre',author:(d.author_name||['Auteur inconnu'])[0],cover:d.cover_i?`https://covers.openlibrary.org/b/id/${d.cover_i}-M.jpg`:''})))}catch{render(seeds);alert('Recherche indisponible.')} }function init(){render(seeds);const q=$('q');q?.addEventListener('keydown',e=>{if(e.key==='Enter'){e.preventDefault();search()}});const s=document.querySelector('[data-action="search"]');if(s)s.onclick=e=>{e.preventDefault();search()};const surprise=document.querySelector('[data-action="surprise"]');if(surprise)surprise.onclick=e=>{e.preventDefault();S.selected=[...seeds].sort(()=>Math.random()-.5).slice(0,3);render(seeds);alert('Sélection surprise prête.')};const build=document.querySelector('[data-action="build"]');if(build)build.onclick=e=>{e.preventDefault();if(S.selected.length<3)return alert('Choisis au moins 3 livres.');alert('Profil créé à partir de tes '+S.selected.length+' livres.')};}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init()})();